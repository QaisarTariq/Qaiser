@model Task.tbl_Task

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>tbl_Task</legend>

        <div class="editor-label">
            @Html.Label("Title")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.strTitle)
            @Html.ValidationMessageFor(model => model.strTitle)
        </div>

        <div class="editor-label">
            @Html.Label("Descripition")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.strDescripition)
            @Html.ValidationMessageFor(model => model.strDescripition)
        </div>

        <div class="editor-label">
            @Html.Label("Due Date")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.dtDueDate)
            @Html.ValidationMessageFor(model => model.dtDueDate)
        </div>

        <div class="editor-label">
            @Html.Label("Required Hours")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.requiredHours)
            @Html.ValidationMessageFor(model => model.requiredHours)
        </div>

        <div class="editor-label">
            @Html.Label("Priority")
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.priority)
            @Html.ValidationMessageFor(model => model.priority)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
